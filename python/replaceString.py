import sys
import os

def main():
    
    rootDir = "../scripts"
    backupDir = "scriptBackup"
    findStr = "__PX__"
    replaceStr = "__toPx__"
    
    
    # make a backup folder
    # clear it
    #if (not os.path.exists(os.path.join("./", backupDir))):
    #    os.mkdir(os.path.join("./", backupDir))
        
    


if __name__ == "__main__":
    main()